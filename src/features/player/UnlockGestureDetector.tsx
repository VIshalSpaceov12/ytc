import { View } from 'react-native';
import type { UnlockGesture } from '@/shared/types/kidProfile';
type Props = { gesture: UnlockGesture };
export function UnlockGestureDetector(_: Props) { return <View />; }
